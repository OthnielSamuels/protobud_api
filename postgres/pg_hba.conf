# TYPE  DATABASE   USER       ADDRESS          METHOD

# Docker Compose internal network only
host    protobot   protobot   172.16.0.0/12    md5

# Local loopback (for pg_isready healthcheck and CLI access)
local   all        all                         trust
host    all        all        127.0.0.1/32     md5