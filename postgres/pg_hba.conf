# Allow password-authenticated connections from the Docker Compose network.
# This is safe for local development; adjust or tighten for production.

# TYPE  DATABASE        USER            ADDRESS                 METHOD
host    all             all             0.0.0.0/0               md5
host    all             all             ::/0                    md5
