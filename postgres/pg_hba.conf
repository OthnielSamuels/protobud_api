# TYPE  DATABASE   USER       ADDRESS          METHOD

# Docker Compose internal network + Docker Desktop host gateway (172.x)
host    all        all        172.16.0.0/12    md5

# Local loopback (for pg_isready healthcheck and CLI access)
local   all        all                         trust
host    all        all        127.0.0.1/32     md5